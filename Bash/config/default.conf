#!/bin/bash
#
# Default configuration for Bash Scripts Collection
# File: config/default.conf
# Author: Kacper
# Version: 1.0
#

# System Monitoring Settings
DISK_ALERT_THRESHOLD=80          # Disk usage alert threshold (%)
CPU_ALERT_THRESHOLD=80           # CPU usage alert threshold (%)  
RAM_ALERT_THRESHOLD=90           # RAM usage alert threshold (%)
ALERT_LOG_FILE="/tmp/system_alerts.log"

# Backup Settings
BACKUP_RETENTION_DAYS=7          # Keep backups for X days
BACKUP_COMPRESSION="gzip"        # Compression type: gzip, bzip2, xz
BACKUP_EXCLUDE_PATTERNS=("*.tmp" "*.log" ".git")

# Network Settings  
NETWORK_TIMEOUT=30               # Network timeout in seconds
MAX_RETRIES=3                    # Maximum retry attempts

# Password Generator Settings
DEFAULT_PASSWORD_LENGTH=12       # Default password length
MIN_PASSWORD_LENGTH=4            # Minimum password length
MAX_PASSWORD_LENGTH=50           # Maximum password length

# File Management Settings
DEFAULT_DIRECTORY="."            # Default directory for file operations
SHOW_HIDDEN_FILES=false          # Show hidden files by default
